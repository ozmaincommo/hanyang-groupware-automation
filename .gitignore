__pycache__/
*.pyc
credentials.local.json
error.log

# 실행 결과물 — 실제 직원 개인정보(이름/사번/부서/결재자/근무기록 등)가 담긴
# 스크린샷·카탈로그 JSON이 섞여 있어 git 이력에 영구히 남기지 않는다.
# gwauto discover-shortcuts / recon-all 등으로 언제든 다시 만들 수 있다.
output/
